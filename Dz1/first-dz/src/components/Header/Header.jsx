import React from 'react';
import playing from './Header.module.css'
function Header(props) {
    return (
        <div className={playing.hand}>
            это Header
        </div>
    );
}

export default Header;